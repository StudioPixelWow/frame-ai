# חיבור Meta לאופטימיזציה אמיתית — מדריך System User

המטרה: לאפשר למערכת **לבצע שינויים בפועל** בקמפיינים (השהיה, שינוי תקציב, הרחבת קהל) ולא רק להציג נתונים. הפתרון הנכון לסוכנות הוא **System User token** מ‑Business Manager — טוקן קבוע (לא פג תוקף) שמתאים לאוטומציה שרת‑לשרת.

> חשוב לדעת מראש: כתיבה ל‑Meta **לא חסומה בגלל אימות העסק כשלעצמו**. כל עוד ל‑System User יש הרשאת `ads_management` ותפקיד אדמין על חשבון המודעות — אפשר לבצע שינויים כבר עכשיו, רק בתקרת קצב נמוכה (Development tier). אימות העסק + App Review נחוצים בעיקר כדי לעלות ל‑Standard tier (תקרות גבוהות, ~25–50 חשבונות לקוח).

---

## שלב 1 — צור System User ב‑Business Manager

1. היכנס ל‑**business.facebook.com** → ⚙️ **Business Settings**.
2. בתפריט הימני: **Users → System Users**.
3. לחץ **Add** → תן שם (למשל `frame-ai-automation`) → תפקיד **Admin** → **Create**.

## שלב 2 — צרף את חשבונות המודעות ל‑System User

1. עדיין ב‑Business Settings → **Accounts → Ad Accounts**.
2. ודא שכל חשבון מודעות שאתה מנהל מופיע כאן. אם לא — **Add** (או בקש מהלקוח לשתף את החשבון ל‑Business Manager שלך דרך **Partners**).
3. חזור ל‑**System Users** → בחר את ה‑System User שיצרת → **Assign Assets** → **Ad Accounts** → סמן את החשבונות → הרשאה **Manage campaigns / Full control** → **Save**.

## שלב 3 — הפק טוקן עם ההרשאות הנכונות

1. ב‑**System Users** → בחר את המשתמש → **Generate New Token**.
2. בחר את ה‑**App** שלך (זו שמחוברת ל‑frame‑ai).
3. **Token expiration: Never** (זו כל הנקודה — טוקן קבוע).
4. סמן את ההרשאות:
   - `ads_management` ← חובה לכתיבה (השהיה/תקציב)
   - `ads_read` ← קריאת ביצועים
   - `business_management` ← מומלץ לניהול נכסים
5. **Generate Token** → **העתק ושמור את הטוקן** (הוא מוצג פעם אחת בלבד).

## שלב 4 — הדבק את הטוקן במערכת

הדבק את הטוקן בהגדרות ה‑Meta של frame‑ai (אותו מקום שבו מחברים את החשבון). הוא נשמר ב‑`app_settings` תחת המפתח `meta_business_token` ומשמש את כל הפעולות.

## שלב 5 — הרץ אבחון

בעמוד **קמפיינים — Meta Ads** לחץ **🔍 בדיקת חיבור ואבחון אופטימיזציה**. הבדיקה:
- מוודאת שהטוקן תקף,
- בודקת שההרשאות כוללות `ads_management`,
- מוודאת גישה לחשבון המודעות,
- ומריצה **בדיקת כתיבה אמיתית** (משנה שם של קמפיין לשם של עצמו — בלי כל אפקט).

אם רואים **✅ כתיבה ל‑Meta עובדת** — הכול מוכן. אם לא — האבחון יציג בדיוק מה חסר ומה לתקן.

---

## מצב הפעלה: "המלצה + אישור" מול "אוטומטי"

באותו עמוד יש מתג **מצב אופטימיזציה**:

- **המלצה + אישור** (ברירת מחדל) — המנוע מחשב את השינויים ושולח אותם ל**תור האישורים** (עמוד *אישורים → פעולות קמפיין*). אתה מאשר בקליק, או מיישם ידנית ב‑Ads Manager. **שום שינוי לא נשלח ל‑Meta אוטומטית.** מתאים בדיוק לתקופה שבה הכתיבה עדיין לא מאומתת — כך כל מה שבנינו ממשיך לתת ערך (המלצות מדויקות + התראות) בלי סיכון.
- **אוטומטי** — המנוע מבצע את השינויים ישירות ב‑Meta. עבור רק אחרי שהאבחון מראה ✅.

---

## נפח ותקרות (לידיעה)

- **Development tier** (לפני App Review/אימות): כתיבה עובדת על חשבונות שלך, אך בתקרת קצב נמוכה — מספיק למחזור אופטימיזציה אחד‑שניים ביום.
- **Standard tier** (אחרי App Review + Business Verification): ~9,000 נקודות, מתאים ל‑25–50 חשבונות לקוח ולאוטומציות תכופות.

## שחרור האימות שתקוע

אם אימות העסק "In review" זמן רב — לרוב הסיבה היא **אי‑התאמת שם** בין שם העסק ב‑Business Manager (מאיה) לשם במסמכי האימות (סטודיו פיקסל). יישר את השמות (או הגש מסמך שתואם לשם ב‑BM) והגש מחדש.
