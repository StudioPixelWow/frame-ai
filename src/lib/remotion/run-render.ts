/**
 * DEPRECATED — This file is no longer used.
 * Remotion rendering runs in the external worker process (worker.ts),
 * NOT inside Next.js routes.
 *
 * Start the worker with: npx tsx src/lib/render-worker/worker.ts
 */
export {};
